"use client";

import { useAppContext } from "./contexto/AppContexto";

export default function Home(){
  return (
    const {nombre} = useAppContext();
    <div>
    <p>hola{nombre}</p>
    </div>
  );
}